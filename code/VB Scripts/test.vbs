go=1
while (go=1) 
	a=int(200*rnd(1))
	answer=false
	while answer=false
		bb=inputbox("What your guess 1-200")

		msgbox(b)
		if b<a then msgbox("Too Low")
		if b>a then msgbox("Too High")
		if b=a then
			msgbox("right")
			answer=true
		end if
		answer=true
	wend
		d=msgbox("another number",4,"fuck you fucker")
		u=str(d)
		msgbox(u)
		go=0
wend

	